/* if we need to find all children elements(including text), we can use:

   const childrenElements = document.querySelector('.container').childNodes;
*/

const childrenElements = document.querySelector('.container').children;
